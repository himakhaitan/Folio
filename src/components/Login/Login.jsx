import classes from "./Login.module.css";

const Login = () => {
  return (
    <div className={classes.login}>
      {/* Logo */}
      <div className={classes.logo_container}>
        <img className={classes.logo} src='/ui_pack/folio.png' alt='Folio Logo' />
      </div>

      {/* Divs */}
      <div></div>
    </div>
  );
};

export default Login;
